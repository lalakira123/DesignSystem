{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^3.0.1",
    "turbo": "^1.7.0",
    "vite": "^4.0.4"
  }
}
